// Example Firebase config. Create a Firebase project, enable Firestore + Storage,
// then copy your config here and rename file to firebase-config.js
// DO NOT commit your real credentials to a public repo without proper rules.

const firebaseConfig = {
  apiKey: "YOUR_API_KEY",
  authDomain: "YOUR_PROJECT_ID.firebaseapp.com",
  projectId: "YOUR_PROJECT_ID",
  storageBucket: "YOUR_PROJECT_ID.appspot.com",
  messagingSenderId: "SENDER_ID",
  appId: "APP_ID"
};